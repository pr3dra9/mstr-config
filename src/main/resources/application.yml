## YAML Template.
---
spring:
  #application.name: config
  #profiles.active: native
  #cloud:
    #config:
      #server.native.searchLocations: file:./src/main/resources/config-repo
      #uri: http://localhost:8888
  devtools.restart.enabled: false

---
spring:
  config.activate.on-profile: docker

  #cloud.config:
    #server.native.searchLocations: file:/app/config-repo
    #uri: http://config:8080
